// name: BLACK HOLE
// category: space
// ported from prism-video-synth
float hash(vec2 p){return fract(sin(dot(p,vec2(127.1,311.7)))*43758.5453);}void main(){vec2 uv=(gl_FragCoord.xy-u_res.xy*0.5)/u_res.y;float t=u_time*u_speed;vec2 p=uv*1.5*u_scale;float r=length(p);float a=atan(p.y,p.x);float aw=a+0.7/(r+0.12)+t*0.25;float rs=length(vec2(p.x,p.y*3.4));float disk=smoothstep(0.22,0.3,rs)*smoothstep(1.15,0.45,rs);float bands=0.6+0.4*sin(rs*16.0-aw*2.0-t*2.2);float doppler=1.25+0.75*sin(a+0.6);vec3 dc=mix(vec3(1.0,0.45,0.15),vec3(0.7,0.8,1.0),clamp(doppler-0.8,0.0,1.0));vec3 col=dc*disk*bands*doppler*0.85;float photon=exp(-abs(r-0.235)*55.0);col+=vec3(1.0,0.85,0.6)*photon*1.6;col*=smoothstep(0.2,0.225,r);vec2 sp=p+0.35*p/(dot(p,p)+0.12);col+=vec3(1.0)*step(0.992,hash(floor(sp*60.0)))*0.45*smoothstep(0.25,0.4,r);col+=vec3(0.01,0.01,0.03);outColor=vec4(audioPop(col*u_intensity),1.0);}
