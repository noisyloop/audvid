// name: FRACTAL PINWHEEL
// category: psychedelic
// ported from prism-video-synth
float hash(vec2 p){return fract(sin(dot(p,vec2(127.1,311.7)))*43758.5453);}mat2 rot(float a){float c=cos(a),s=sin(a);return mat2(c,-s,s,c);}void main(){vec2 uv=(gl_FragCoord.xy-u_res.xy*0.5)/u_res.y;float t=u_time*u_speed;vec2 p=uv*u_scale;float r=length(p);float a=atan(p.y,p.x);vec2 lp=vec2(log(r+1e-5)*1.2-t*0.4,a*1.90986);lp.y+=lp.x*0.4;vec2 id=floor(lp);vec2 f=fract(lp)-0.5;float h=hash(id);f=rot(h*6.28318+t*(h-0.5)*1.5)*f;float blades=pow(abs(sin(atan(f.y,f.x)*2.0+t*h)),4.0)*smoothstep(0.5,0.1,length(f));vec3 col=(0.5+0.5*cos(6.28318*(h+lp.x*0.08+t*0.06+vec3(0.0,0.33,0.67))))*blades*1.3;col+=(0.5+0.5*cos(6.28318*(lp.x*0.1+vec3(0.5,0.83,1.17))))*0.04/(abs(length(f)-0.42)+0.05);outColor=vec4(audioPop(col*u_intensity),1.0);}
