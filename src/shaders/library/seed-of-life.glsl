// name: SEED OF LIFE
// category: sacred
// ported from prism-video-synth
void main(){vec2 uv=(gl_FragCoord.xy-u_res.xy*0.5)/u_res.y;float t=u_time*u_speed;vec2 p=uv*2.4*u_scale;float ca=cos(t*0.1),sa=sin(t*0.1);p=mat2(ca,-sa,sa,ca)*p;float R=0.55;float phase=mod(t*0.5,10.0);float fade=smoothstep(10.0,8.5,phase);float v=0.0;for(int i=0;i<7;i++){float fi=float(i);vec2 c=fi<0.5?vec2(0.0):R*vec2(cos((fi-1.0)*1.0472),sin((fi-1.0)*1.0472));float grow=clamp(phase-fi*0.9,0.0,1.0);grow=grow*grow*(3.0-2.0*grow);float d=abs(length(p-c)-R*grow);v+=(smoothstep(0.012,0.0,d)+0.004/(d+0.015))*step(0.01,grow);}v*=fade;float hue=atan(p.y,p.x)/6.28318+length(p)*0.25+t*0.08;vec3 col=(0.5+0.5*cos(6.28318*(hue+vec3(0.0,0.33,0.67))))*v;col+=vec3(0.15,0.05,0.25)*(sin(length(p)*7.0-t*1.5)*0.5+0.5)*(1.0-min(v,1.0))*0.5;outColor=vec4(col*u_intensity,1.0);}
